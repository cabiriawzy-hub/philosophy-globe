# P1 · 骨架实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 跑通"哲学思想宇宙"的最小可分享版本 —— Vite + React + TS 脚手架 + react-globe.gl 深色 3D 地球 + 10 个轴心时代 emoji 思想点 + hover 卡片 + Vercel 公网 URL。

**Architecture:** 纯前端静态应用。Vite 构建，React 18 组件化，Zustand 管 hover 状态，react-globe.gl 渲染 3D 地球。数据先硬编码在 JSON 文件，后续阶段扩充。GitHub + Vercel 自动部署。

**Tech Stack:** Vite 5 · React 18 · TypeScript 5 · react-globe.gl · three · Zustand · Vitest · Vercel

**本计划范围：仅 P1。** P2~P6 每一期各自写独立计划。

**工作目录：** `/Users/bytedance/philosophy-map`

---

## 文件结构（P1 结束时）

```
philosophy-map/
├── .gitignore
├── .nvmrc                        # 锁 Node 版本
├── README.md                     # 项目说明 + 部署 URL
├── package.json
├── tsconfig.json
├── tsconfig.node.json            # Vite 配置文件的 TS config
├── vite.config.ts
├── vitest.config.ts
├── index.html
├── vercel.json                   # Vercel 构建配置（可选，但显式更清楚）
├── public/
│   └── data/
│       └── thoughts-p1.json      # 10 条硬编码思想点
├── src/
│   ├── main.tsx                  # 入口
│   ├── App.tsx                   # 根组件
│   ├── App.module.css
│   ├── styles/
│   │   └── globals.css           # 深色主题 + 字体 + reset
│   ├── types/
│   │   └── thought.ts            # Thought 类型（P1 精简版）
│   ├── data/
│   │   ├── loadThoughts.ts       # 异步加载 JSON
│   │   └── loadThoughts.test.ts
│   ├── state/
│   │   ├── useStore.ts           # Zustand store（hoveredThought）
│   │   └── useStore.test.ts
│   └── components/
│       ├── Globe/
│       │   ├── Globe.tsx         # react-globe.gl 封装
│       │   └── Globe.module.css
│       └── Tooltip/
│           ├── Tooltip.tsx       # hover 卡片
│           ├── Tooltip.module.css
│           └── Tooltip.test.tsx
└── docs/
    └── superpowers/
        ├── specs/
        │   └── 2026-04-17-philosophy-globe-design.md   (已存在)
        └── plans/
            └── 2026-04-17-p1-skeleton.md               (本文件)
```

---

## Task 1: 初始化 Git 仓库 + .gitignore

**Files:**
- Create: `/Users/bytedance/philosophy-map/.gitignore`
- Create: `/Users/bytedance/philosophy-map/.nvmrc`

- [ ] **Step 1: 进入项目目录并初始化 git**

```bash
cd /Users/bytedance/philosophy-map
git init -b main
```

Expected: `Initialized empty Git repository in /Users/bytedance/philosophy-map/.git/`

- [ ] **Step 2: 创建 `.gitignore`**

内容：

```gitignore
# deps
node_modules/
.pnp
.pnp.js

# build
dist/
dist-ssr/
*.local

# editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# env
.env
.env.local
.env.*.local

# test
coverage/
.nyc_output/

# superpowers workspace（brainstorming session、plans、specs 保留在 docs/）
.superpowers/

# vercel
.vercel
```

- [ ] **Step 3: 创建 `.nvmrc`**

内容：

```
22
```

- [ ] **Step 4: 首次提交**

```bash
cd /Users/bytedance/philosophy-map
git add .gitignore .nvmrc docs/
git commit -m "chore: init repo with gitignore, nvmrc, and design docs"
```

Expected: commit 成功，包含 `.gitignore`、`.nvmrc`、`docs/superpowers/specs/...`、`docs/superpowers/plans/...`。

---

## Task 2: Vite + React + TypeScript 脚手架

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `tsconfig.node.json`
- Create: `vite.config.ts`
- Create: `index.html`
- Create: `src/main.tsx`
- Create: `src/App.tsx`
- Create: `src/App.module.css`
- Create: `src/vite-env.d.ts`

- [ ] **Step 1: 创建 `package.json`**

```json
{
  "name": "philosophy-globe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
```

- [ ] **Step 2: 创建 `tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "resolveJsonModule": true,
    "baseUrl": ".",
    "paths": { "@/*": ["src/*"] }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

- [ ] **Step 3: 创建 `tsconfig.node.json`**

```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts", "vitest.config.ts"]
}
```

- [ ] **Step 4: 创建 `vite.config.ts`**

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'node:path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: { '@': path.resolve(__dirname, 'src') }
  },
  server: { port: 5173 }
})
```

- [ ] **Step 5: 创建 `index.html`**

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#05080f" />
    <title>哲学思想宇宙 · Philosophy Globe</title>
    <style>
      html, body, #root { margin: 0; padding: 0; height: 100%; background: #05080f; }
    </style>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 6: 创建 `src/vite-env.d.ts`**

```ts
/// <reference types="vite/client" />
```

- [ ] **Step 7: 创建 `src/main.tsx`**

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './styles/globals.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

- [ ] **Step 8: 创建 `src/App.tsx`**

```tsx
import styles from './App.module.css'

export default function App() {
  return (
    <div className={styles.app}>
      <h1 className={styles.placeholder}>Philosophy Globe · 启动中</h1>
    </div>
  )
}
```

- [ ] **Step 9: 创建 `src/App.module.css`**

```css
.app {
  width: 100%;
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
}

.placeholder {
  font-size: 18px;
  opacity: 0.5;
  font-weight: 300;
  letter-spacing: 0.05em;
}
```

- [ ] **Step 10: 创建占位 `src/styles/globals.css`**

（真正的主题变量在 Task 3 填入。这里先放空文件以便 main.tsx import 不报错。）

```css
/* 主题变量和 reset 在 Task 3 填入 */
```

- [ ] **Step 11: 安装依赖**

```bash
cd /Users/bytedance/philosophy-map
npm install
```

Expected: `node_modules/` 出现；`package-lock.json` 生成；无 error。

- [ ] **Step 12: 启动 dev server 验证**

```bash
npm run dev
```

在浏览器打开 `http://localhost:5173`，应该看到深色背景 + 灰色文字 "Philosophy Globe · 启动中"。确认后 Ctrl+C 停止。

- [ ] **Step 13: 类型检查**

```bash
npm run typecheck
```

Expected: 无 error。

- [ ] **Step 14: 提交**

```bash
cd /Users/bytedance/philosophy-map
git add package.json package-lock.json tsconfig.json tsconfig.node.json vite.config.ts index.html src/
git commit -m "chore: scaffold Vite + React + TypeScript"
```

---

## Task 3: 深色主题 globals.css

**Files:**
- Modify: `src/styles/globals.css`

- [ ] **Step 1: 填入完整 `src/styles/globals.css`**

替换整个文件内容为：

```css
:root {
  --bg-deep: #05080f;
  --bg-surface: #0a0f1c;
  --bg-elevated: #111829;
  --fg-primary: #e8ecf5;
  --fg-secondary: rgba(232, 236, 245, 0.7);
  --fg-tertiary: rgba(232, 236, 245, 0.45);
  --accent: #6aa9ff;
  --accent-glow: rgba(106, 169, 255, 0.35);
  --border: rgba(255, 255, 255, 0.1);

  --font-cn: "Source Han Serif SC", "Noto Serif CJK SC", "Songti SC", serif;
  --font-en: "Inter", -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  --font-mono: "JetBrains Mono", "SF Mono", Menlo, monospace;
}

*, *::before, *::after {
  box-sizing: border-box;
}

html, body, #root {
  margin: 0;
  padding: 0;
  height: 100%;
  background: var(--bg-deep);
  color: var(--fg-primary);
  font-family: var(--font-en), var(--font-cn);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  overflow: hidden;
}

button {
  font-family: inherit;
  color: inherit;
  background: none;
  border: none;
  padding: 0;
  cursor: pointer;
}

a { color: var(--accent); text-decoration: none; }
a:hover { text-decoration: underline; }
```

- [ ] **Step 2: 启动 dev server 验证**

```bash
npm run dev
```

浏览器打开 `http://localhost:5173`，占位文字应为淡白色（#e8ecf5），背景仍是 `#05080f`。Ctrl+C 停止。

- [ ] **Step 3: 提交**

```bash
git add src/styles/globals.css
git commit -m "feat: add dark theme globals and CSS variables"
```

---

## Task 4: Thought 类型 + 10 条硬编码数据

**Files:**
- Create: `src/types/thought.ts`
- Create: `public/data/thoughts-p1.json`

- [ ] **Step 1: 创建 `src/types/thought.ts`**

```ts
/**
 * P1 精简版 Thought 类型。完整类型将在 P2 扩展（concepts、influences、tier 等）。
 */
export interface ThoughtP1 {
  id: string
  emoji: string
  nameZh: string
  nameEn: string
  philosopher: string
  year: number
  location: {
    lat: number
    lng: number
    placeName: string
  }
  shortDescription: string
}
```

- [ ] **Step 2: 创建 `public/data/thoughts-p1.json`**

10 条轴心时代思想点，覆盖希腊、中国、印度、波斯。

```json
[
  {
    "id": "socrates-elenchus",
    "emoji": "💬",
    "nameZh": "苏格拉底诘问法",
    "nameEn": "Socratic Elenchus",
    "philosopher": "苏格拉底",
    "year": -400,
    "location": { "lat": 37.9838, "lng": 23.7275, "placeName": "雅典" },
    "shortDescription": "通过不断追问检验对方信念，让"自认为懂"的人暴露矛盾，再自己走向真知。哲学作为对话从此诞生。"
  },
  {
    "id": "plato-forms",
    "emoji": "🏛️",
    "nameZh": "理念论",
    "nameEn": "Theory of Forms",
    "philosopher": "柏拉图",
    "year": -380,
    "location": { "lat": 37.9908, "lng": 23.7033, "placeName": "雅典学院" },
    "shortDescription": "我们眼中的世界是影子，真正的实在是超越时空的"理念"。桌子是桌子，是因为分享了"桌子的理念"。"
  },
  {
    "id": "aristotle-virtue-ethics",
    "emoji": "❤️",
    "nameZh": "德性伦理学",
    "nameEn": "Virtue Ethics",
    "philosopher": "亚里士多德",
    "year": -340,
    "location": { "lat": 37.9908, "lng": 23.7033, "placeName": "吕克昂" },
    "shortDescription": "幸福（eudaimonia）不是一种感觉，而是一生都按德性行事的状态。中庸是核心：勇敢在鲁莽和怯懦之间。"
  },
  {
    "id": "confucius-ren",
    "emoji": "🤝",
    "nameZh": "仁",
    "nameEn": "Ren (Benevolence)",
    "philosopher": "孔子",
    "year": -500,
    "location": { "lat": 35.5970, "lng": 116.9927, "placeName": "曲阜" },
    "shortDescription": "仁者爱人。克己复礼为仁。道德的根基不是神，不是法，是人对人的真情——"己所不欲，勿施于人"。"
  },
  {
    "id": "laozi-wuwei",
    "emoji": "🍃",
    "nameZh": "无为",
    "nameEn": "Wu Wei (Non-action)",
    "philosopher": "老子",
    "year": -550,
    "location": { "lat": 34.7466, "lng": 113.6253, "placeName": "郑州（传说出生地）" },
    "shortDescription": "最高明的治理不是强行干预，而是顺应自然。道生万物而不居功。水看起来最柔弱，却能穿透一切。"
  },
  {
    "id": "mozi-jianai",
    "emoji": "🕊️",
    "nameZh": "兼爱",
    "nameEn": "Universal Love",
    "philosopher": "墨子",
    "year": -450,
    "location": { "lat": 34.7466, "lng": 113.6253, "placeName": "宋国" },
    "shortDescription": "爱所有人，如同爱自己的亲人。对儒家"爱有差等"的直接反驳。是东亚最早的普世主义伦理。"
  },
  {
    "id": "buddha-four-noble-truths",
    "emoji": "🧘",
    "nameZh": "四圣谛",
    "nameEn": "Four Noble Truths",
    "philosopher": "释迦牟尼",
    "year": -528,
    "location": { "lat": 24.6951, "lng": 84.9914, "placeName": "菩提伽耶" },
    "shortDescription": "苦、集、灭、道。人生本苦，苦源于欲，欲可灭尽，路径是八正道。整个佛教哲学的公理系统。"
  },
  {
    "id": "upanishads-atman-brahman",
    "emoji": "🌌",
    "nameZh": "梵我合一",
    "nameEn": "Atman = Brahman",
    "philosopher": "奥义书传统",
    "year": -700,
    "location": { "lat": 25.3176, "lng": 82.9739, "placeName": "瓦拉纳西" },
    "shortDescription": "个体灵魂（Atman）与宇宙本源（Brahman）本质上是同一个。所谓修行，就是认出这个同一性。"
  },
  {
    "id": "zarathustra-dualism",
    "emoji": "🔥",
    "nameZh": "善恶二元",
    "nameEn": "Cosmic Dualism",
    "philosopher": "琐罗亚斯德",
    "year": -1000,
    "location": { "lat": 37.5665, "lng": 61.1788, "placeName": "古波斯（巴尔赫）" },
    "shortDescription": "宇宙是光明神与黑暗神的战场。人的每个选择都在加入其中一方。后来深刻影响犹太-基督教传统。"
  },
  {
    "id": "heraclitus-flux",
    "emoji": "🌊",
    "nameZh": "万物流变",
    "nameEn": "Panta Rhei (Flux)",
    "philosopher": "赫拉克利特",
    "year": -500,
    "location": { "lat": 37.9411, "lng": 27.3416, "placeName": "以弗所" },
    "shortDescription": "人不能两次踏入同一条河。变化是唯一的常态，而推动变化的是"对立的统一"——后来黑格尔辩证法的远祖。"
  }
]
```

- [ ] **Step 3: 提交**

```bash
git add src/types/thought.ts public/data/thoughts-p1.json
git commit -m "feat: add ThoughtP1 type and 10 axial-age sample thoughts"
```

---

## Task 5: 数据加载器 + 测试

**Files:**
- Create: `vitest.config.ts`
- Create: `src/data/loadThoughts.ts`
- Create: `src/data/loadThoughts.test.ts`

- [ ] **Step 1: 创建 `vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'node:path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: { '@': path.resolve(__dirname, 'src') }
  },
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: []
  }
})
```

- [ ] **Step 2: 安装测试依赖**

```bash
npm install -D jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event
```

- [ ] **Step 3: 写失败的测试 `src/data/loadThoughts.test.ts`**

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { loadThoughtsP1 } from './loadThoughts'

describe('loadThoughtsP1', () => {
  beforeEach(() => {
    vi.resetAllMocks()
  })

  it('fetches and returns the thoughts array', async () => {
    const mock = [
      {
        id: 'test-1',
        emoji: '🌊',
        nameZh: '测试',
        nameEn: 'Test',
        philosopher: '某人',
        year: -500,
        location: { lat: 0, lng: 0, placeName: 'X' },
        shortDescription: '描述。'
      }
    ]
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      ok: true,
      json: async () => mock
    }))

    const result = await loadThoughtsP1()
    expect(result).toHaveLength(1)
    expect(result[0].id).toBe('test-1')
    expect(result[0].emoji).toBe('🌊')
  })

  it('throws when fetch fails', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({ ok: false, status: 404 }))
    await expect(loadThoughtsP1()).rejects.toThrow(/404/)
  })
})
```

- [ ] **Step 4: 运行测试确认失败**

```bash
npm run test:run -- src/data/loadThoughts.test.ts
```

Expected: FAIL，报 "Cannot find module './loadThoughts'" 或 "loadThoughtsP1 is not a function"。

- [ ] **Step 5: 实现 `src/data/loadThoughts.ts`**

```ts
import type { ThoughtP1 } from '@/types/thought'

export async function loadThoughtsP1(): Promise<ThoughtP1[]> {
  const response = await fetch('/data/thoughts-p1.json')
  if (!response.ok) {
    throw new Error(`Failed to load thoughts: ${response.status}`)
  }
  return response.json() as Promise<ThoughtP1[]>
}
```

- [ ] **Step 6: 再跑测试确认通过**

```bash
npm run test:run -- src/data/loadThoughts.test.ts
```

Expected: PASS，2 tests passed。

- [ ] **Step 7: 提交**

```bash
git add vitest.config.ts src/data/loadThoughts.ts src/data/loadThoughts.test.ts package.json package-lock.json
git commit -m "feat: add thought loader with test harness"
```

---

## Task 6: Zustand store + 测试

**Files:**
- Create: `src/state/useStore.ts`
- Create: `src/state/useStore.test.ts`

- [ ] **Step 1: 安装 Zustand**

```bash
npm install zustand
```

- [ ] **Step 2: 写失败的测试 `src/state/useStore.test.ts`**

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { useGlobeStore } from './useStore'

describe('useGlobeStore', () => {
  beforeEach(() => {
    useGlobeStore.setState({ hoveredThought: null })
  })

  it('starts with no hovered thought', () => {
    expect(useGlobeStore.getState().hoveredThought).toBeNull()
  })

  it('sets hovered thought', () => {
    const thought = {
      id: 'x',
      emoji: '🌊',
      nameZh: '测试',
      nameEn: 'Test',
      philosopher: '某人',
      year: -500,
      location: { lat: 0, lng: 0, placeName: 'X' },
      shortDescription: '描述'
    }
    useGlobeStore.getState().setHovered(thought)
    expect(useGlobeStore.getState().hoveredThought?.id).toBe('x')
  })

  it('clears hovered thought', () => {
    useGlobeStore.getState().setHovered({
      id: 'y',
      emoji: '🔥',
      nameZh: 'Y',
      nameEn: 'Y',
      philosopher: 'P',
      year: 0,
      location: { lat: 0, lng: 0, placeName: '' },
      shortDescription: ''
    })
    useGlobeStore.getState().setHovered(null)
    expect(useGlobeStore.getState().hoveredThought).toBeNull()
  })
})
```

- [ ] **Step 3: 运行测试确认失败**

```bash
npm run test:run -- src/state/useStore.test.ts
```

Expected: FAIL，报 "Cannot find module './useStore'"。

- [ ] **Step 4: 实现 `src/state/useStore.ts`**

```ts
import { create } from 'zustand'
import type { ThoughtP1 } from '@/types/thought'

interface GlobeStore {
  hoveredThought: ThoughtP1 | null
  setHovered: (thought: ThoughtP1 | null) => void
}

export const useGlobeStore = create<GlobeStore>((set) => ({
  hoveredThought: null,
  setHovered: (thought) => set({ hoveredThought: thought })
}))
```

- [ ] **Step 5: 再跑测试确认通过**

```bash
npm run test:run -- src/state/useStore.test.ts
```

Expected: PASS，3 tests passed。

- [ ] **Step 6: 提交**

```bash
git add src/state/useStore.ts src/state/useStore.test.ts package.json package-lock.json
git commit -m "feat: add Zustand store for hovered thought"
```

---

## Task 7: Globe 组件（react-globe.gl 深色地球 + emoji 点）

**Files:**
- Create: `src/components/Globe/Globe.tsx`
- Create: `src/components/Globe/Globe.module.css`

- [ ] **Step 1: 安装 react-globe.gl + three**

```bash
npm install react-globe.gl three
npm install -D @types/three
```

- [ ] **Step 2: 创建 `src/components/Globe/Globe.module.css`**

```css
.container {
  position: fixed;
  inset: 0;
  width: 100%;
  height: 100%;
  background: radial-gradient(ellipse at center, #0a0f1c 0%, #02040a 100%);
}

.container canvas {
  cursor: grab;
}

.container canvas:active {
  cursor: grabbing;
}
```

- [ ] **Step 3: 创建 `src/components/Globe/Globe.tsx`**

```tsx
import { useEffect, useRef } from 'react'
import GlobeGL, { type GlobeMethods } from 'react-globe.gl'
import type { ThoughtP1 } from '@/types/thought'
import { useGlobeStore } from '@/state/useStore'
import styles from './Globe.module.css'

interface GlobeProps {
  thoughts: ThoughtP1[]
  width: number
  height: number
}

/**
 * react-globe.gl 接收 htmlElementsData，用 htmlElement 回调返回每个点的 DOM。
 * 我们用 emoji 文本作为 DOM node，随 globe 3D 变换自动跟随。
 */
export default function Globe({ thoughts, width, height }: GlobeProps) {
  const globeRef = useRef<GlobeMethods>()
  const setHovered = useGlobeStore((s) => s.setHovered)

  // 初始相机位置：落在 60°E，让欧亚大陆的轴心时代同框
  useEffect(() => {
    if (!globeRef.current) return
    globeRef.current.pointOfView({ lat: 25, lng: 60, altitude: 2.2 }, 0)
    // 启用自动旋转（缓慢）
    const controls = globeRef.current.controls() as {
      autoRotate: boolean
      autoRotateSpeed: number
    }
    controls.autoRotate = true
    controls.autoRotateSpeed = 0.3
  }, [])

  return (
    <div className={styles.container}>
      <GlobeGL
        ref={globeRef}
        width={width}
        height={height}
        backgroundColor="rgba(0,0,0,0)"
        // 深色地球：用 1×1 纯黑像素作为贴图，等效纯色球面（兼容所有 react-globe.gl 版本）
        globeImageUrl="data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNgAAIAAAUAAen63NgAAAAASUVORK5CYII="
        showAtmosphere={true}
        atmosphereColor="#6aa9ff"
        atmosphereAltitude={0.18}
        // 大陆轮廓由 hexPolygons 呈现
        hexPolygonsData={[]}
        // 思想点通过 htmlElementsData 以 DOM 方式渲染（保证 emoji 清晰）
        htmlElementsData={thoughts}
        htmlLat={(d: object) => (d as ThoughtP1).location.lat}
        htmlLng={(d: object) => (d as ThoughtP1).location.lng}
        htmlAltitude={0.01}
        htmlElement={(d: object) => {
          const t = d as ThoughtP1
          const el = document.createElement('div')
          el.textContent = t.emoji
          el.style.fontSize = '20px'
          el.style.filter = 'drop-shadow(0 0 6px rgba(106,169,255,0.6))'
          el.style.cursor = 'pointer'
          el.style.pointerEvents = 'auto'
          el.style.transition = 'transform 0.15s, filter 0.15s'
          el.onmouseenter = () => {
            setHovered(t)
            el.style.transform = 'scale(1.4)'
            el.style.filter = 'drop-shadow(0 0 12px rgba(255,255,255,0.9))'
          }
          el.onmouseleave = () => {
            setHovered(null)
            el.style.transform = 'scale(1)'
            el.style.filter = 'drop-shadow(0 0 6px rgba(106,169,255,0.6))'
          }
          return el
        }}
      />
    </div>
  )
}
```

注意：`globeImageUrl` 传入的是 1×1 黑色像素 data URL，这样球面呈现为深黑色；大气辉光来自 `atmosphereColor="#6aa9ff"`。若想改成真实 Blue Marble 贴图，改成 Three.js 示例的 `//unpkg.com/three-globe/example/img/earth-night.jpg`。

- [ ] **Step 4: 提交骨架（先提交，下一步可能修改）**

```bash
git add src/components/Globe/ package.json package-lock.json
git commit -m "feat: add 3D globe component with emoji thought markers"
```

---

## Task 8: Tooltip 组件 + 测试

**Files:**
- Create: `src/components/Tooltip/Tooltip.tsx`
- Create: `src/components/Tooltip/Tooltip.module.css`
- Create: `src/components/Tooltip/Tooltip.test.tsx`

- [ ] **Step 1: 写失败的测试 `src/components/Tooltip/Tooltip.test.tsx`**

```tsx
import { describe, it, expect, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import { useGlobeStore } from '@/state/useStore'
import Tooltip from './Tooltip'

describe('Tooltip', () => {
  beforeEach(() => {
    useGlobeStore.setState({ hoveredThought: null })
  })

  it('renders nothing when no thought is hovered', () => {
    render(<Tooltip />)
    expect(screen.queryByTestId('tooltip')).toBeNull()
  })

  it('shows thought name, philosopher, and year when hovered', () => {
    useGlobeStore.setState({
      hoveredThought: {
        id: 't',
        emoji: '🌊',
        nameZh: '万物流变',
        nameEn: 'Flux',
        philosopher: '赫拉克利特',
        year: -500,
        location: { lat: 0, lng: 0, placeName: '以弗所' },
        shortDescription: '人不能两次踏入同一条河。'
      }
    })
    render(<Tooltip />)
    expect(screen.getByTestId('tooltip')).toBeInTheDocument()
    expect(screen.getByText('万物流变')).toBeInTheDocument()
    expect(screen.getByText(/赫拉克利特/)).toBeInTheDocument()
    expect(screen.getByText(/-500|公元前 500/)).toBeInTheDocument()
  })

  it('formats BCE years as "公元前 N"', () => {
    useGlobeStore.setState({
      hoveredThought: {
        id: 't',
        emoji: '🏛️',
        nameZh: '理念论',
        nameEn: 'Forms',
        philosopher: '柏拉图',
        year: -380,
        location: { lat: 0, lng: 0, placeName: '' },
        shortDescription: ''
      }
    })
    render(<Tooltip />)
    expect(screen.getByText(/公元前 380/)).toBeInTheDocument()
  })
})
```

还需要 `@testing-library/jest-dom` matchers。创建 setup 文件：

- [ ] **Step 2: 创建 `src/test-setup.ts`**

```ts
import '@testing-library/jest-dom/vitest'
```

- [ ] **Step 3: 更新 `vitest.config.ts` 指向 setup**

替换整个文件为：

```ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'node:path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: { '@': path.resolve(__dirname, 'src') }
  },
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test-setup.ts']
  }
})
```

- [ ] **Step 4: 运行测试确认失败**

```bash
npm run test:run -- src/components/Tooltip/Tooltip.test.tsx
```

Expected: FAIL，报 "Cannot find module './Tooltip'"。

- [ ] **Step 5: 创建 `src/components/Tooltip/Tooltip.module.css`**

```css
.tooltip {
  position: fixed;
  top: 24px;
  left: 50%;
  transform: translateX(-50%);
  padding: 10px 18px;
  background: rgba(10, 15, 28, 0.92);
  border: 1px solid var(--border);
  border-radius: 8px;
  backdrop-filter: blur(8px);
  color: var(--fg-primary);
  font-size: 14px;
  line-height: 1.5;
  pointer-events: none;
  z-index: 10;
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  gap: 10px;
}

.emoji {
  font-size: 20px;
}

.name {
  font-weight: 500;
}

.meta {
  color: var(--fg-secondary);
  font-size: 13px;
}

.divider {
  color: var(--fg-tertiary);
  margin: 0 6px;
}
```

- [ ] **Step 6: 创建 `src/components/Tooltip/Tooltip.tsx`**

```tsx
import { useGlobeStore } from '@/state/useStore'
import styles from './Tooltip.module.css'

function formatYear(year: number): string {
  if (year < 0) return `公元前 ${Math.abs(year)}`
  return `公元 ${year}`
}

export default function Tooltip() {
  const thought = useGlobeStore((s) => s.hoveredThought)
  if (!thought) return null

  return (
    <div className={styles.tooltip} data-testid="tooltip">
      <span className={styles.emoji}>{thought.emoji}</span>
      <span className={styles.name}>{thought.nameZh}</span>
      <span className={styles.divider}>·</span>
      <span className={styles.meta}>{thought.philosopher}</span>
      <span className={styles.divider}>·</span>
      <span className={styles.meta}>{formatYear(thought.year)}</span>
    </div>
  )
}
```

- [ ] **Step 7: 再跑测试确认通过**

```bash
npm run test:run -- src/components/Tooltip/Tooltip.test.tsx
```

Expected: PASS，3 tests passed。

- [ ] **Step 8: 提交**

```bash
git add src/components/Tooltip/ src/test-setup.ts vitest.config.ts
git commit -m "feat: add hover tooltip for thought details"
```

---

## Task 9: 在 App 中组装一切

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/App.module.css`

- [ ] **Step 1: 替换 `src/App.tsx` 全部内容**

```tsx
import { useEffect, useState } from 'react'
import type { ThoughtP1 } from '@/types/thought'
import { loadThoughtsP1 } from '@/data/loadThoughts'
import Globe from '@/components/Globe/Globe'
import Tooltip from '@/components/Tooltip/Tooltip'
import styles from './App.module.css'

export default function App() {
  const [thoughts, setThoughts] = useState<ThoughtP1[]>([])
  const [loadError, setLoadError] = useState<string | null>(null)
  const [size, setSize] = useState({
    width: window.innerWidth,
    height: window.innerHeight
  })

  useEffect(() => {
    loadThoughtsP1()
      .then(setThoughts)
      .catch((err: Error) => setLoadError(err.message))
  }, [])

  useEffect(() => {
    const onResize = () =>
      setSize({ width: window.innerWidth, height: window.innerHeight })
    window.addEventListener('resize', onResize)
    return () => window.removeEventListener('resize', onResize)
  }, [])

  if (loadError) {
    return (
      <div className={styles.error}>
        数据加载失败：{loadError}
      </div>
    )
  }

  if (thoughts.length === 0) {
    return <div className={styles.loading}>加载思想宇宙中…</div>
  }

  return (
    <div className={styles.app}>
      <Globe thoughts={thoughts} width={size.width} height={size.height} />
      <Tooltip />
      <header className={styles.header}>
        <h1 className={styles.title}>
          哲学思想宇宙
          <span className={styles.subtitle}>
            {thoughts.length} 思想 · 轴心时代起步
          </span>
        </h1>
      </header>
      <footer className={styles.footer}>
        P1 骨架 · 数据源：自生成 · 地球：react-globe.gl
      </footer>
    </div>
  )
}
```

- [ ] **Step 2: 替换 `src/App.module.css` 全部内容**

```css
.app {
  position: relative;
  width: 100%;
  height: 100vh;
  overflow: hidden;
}

.loading, .error {
  width: 100%;
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--fg-secondary);
  font-size: 16px;
  letter-spacing: 0.05em;
}

.error {
  color: #ff8a8a;
}

.header {
  position: fixed;
  top: 0;
  left: 0;
  padding: 20px 24px;
  z-index: 5;
  pointer-events: none;
}

.title {
  margin: 0;
  font-size: 16px;
  font-weight: 500;
  color: var(--fg-primary);
  font-family: var(--font-cn);
  letter-spacing: 0.05em;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.subtitle {
  font-size: 12px;
  font-weight: 300;
  color: var(--fg-tertiary);
  letter-spacing: 0.1em;
}

.footer {
  position: fixed;
  bottom: 12px;
  left: 24px;
  font-size: 11px;
  color: var(--fg-tertiary);
  letter-spacing: 0.05em;
  z-index: 5;
  pointer-events: none;
}
```

- [ ] **Step 3: 本地启动验证**

```bash
npm run dev
```

打开 `http://localhost:5173`。验证清单（用嘴默念 / 对照检查）：

1. 深色背景 + 一个深蓝色 3D 球体（带淡蓝大气辉光）
2. 球体缓慢自转
3. 球面上散布 10 个彩色 emoji（💬🏛️❤️🤝🍃🕊️🧘🌌🔥🌊）
4. 左上角标题"哲学思想宇宙 · 10 思想 · 轴心时代起步"
5. 左下角小字 footer
6. 鼠标悬停某个 emoji → 放大 + 顶部出现黑色浮层卡片 "emoji 名字 · 哲学家 · 公元前 X"
7. 鼠标拖动可旋转地球（停止自转）
8. 滚轮缩放

验证通过后 Ctrl+C 停止。

- [ ] **Step 4: 跑所有测试确认整体 OK**

```bash
npm run test:run
```

Expected: 所有 tests 通过（目前应有 8 个）。

- [ ] **Step 5: 跑类型检查**

```bash
npm run typecheck
```

Expected: 无 error。

- [ ] **Step 6: 跑生产构建确认无 error**

```bash
npm run build
```

Expected: 构建成功，`dist/` 目录生成。

- [ ] **Step 7: 提交**

```bash
git add src/App.tsx src/App.module.css
git commit -m "feat: wire App with globe, tooltip, data loading, and header"
```

---

## Task 10: 创建 README.md

**Files:**
- Create: `README.md`

- [ ] **Step 1: 创建 `README.md`**

```markdown
# 哲学思想宇宙 · Philosophy Globe

用 3D 地球仪 + 时间轴，可视化人类历史至今的所有重要哲学思想。

**当前阶段：P1 · 骨架**（10 个轴心时代思想，hover 可见详情）

👉 在线演示：待 Vercel 部署后填入

## 本地运行

```bash
npm install
npm run dev
```

打开 http://localhost:5173

## 构建

```bash
npm run build
npm run preview
```

## 测试

```bash
npm run test       # watch 模式
npm run test:run   # 单次
npm run typecheck
```

## 文档

- [设计规范](docs/superpowers/specs/2026-04-17-philosophy-globe-design.md)
- [P1 实施计划](docs/superpowers/plans/2026-04-17-p1-skeleton.md)

## 技术栈

- Vite · React · TypeScript
- react-globe.gl (Three.js)
- Zustand
- Vitest + @testing-library/react

## License

MIT · 数据来源遵循各自许可（Wikipedia CC-BY-SA、Twemoji CC-BY 4.0、Natural Earth PD）
```

- [ ] **Step 2: 提交**

```bash
git add README.md
git commit -m "docs: add README with setup and project overview"
```

---

## Task 11: 推送到 GitHub

**Files:**（无文件变更，仅远端操作）

- [ ] **Step 1: 创建 GitHub 仓库并推送**

```bash
cd /Users/bytedance/philosophy-map
gh repo create philosophy-globe --public --source=. --remote=origin --description "3D globe of humanity's philosophical ideas" --push
```

Expected: 输出 `https://github.com/<username>/philosophy-globe`（`<username>` 即 `cabiriawzy-hub`）。提交被推送到 `main`。

- [ ] **Step 2: 验证**

```bash
gh repo view --web
```

浏览器会打开仓库页面，应能看到所有文件和提交历史。

---

## Task 12: 部署到 Vercel

**Files:**
- Create: `vercel.json`

- [ ] **Step 1: 创建 `vercel.json`**

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite"
}
```

- [ ] **Step 2: 提交**

```bash
git add vercel.json
git commit -m "chore: add vercel.json for deployment"
git push
```

- [ ] **Step 3: 首次登录 Vercel CLI**

```bash
npx vercel login
```

会提示用 email / GitHub / GitLab 登录，选 GitHub。浏览器弹出完成授权。

- [ ] **Step 4: 部署**

```bash
npx vercel --yes
```

第一次会问 project 配置，默认值即可（directory 为当前、framework Vite 自动检测）。

Expected: 输出形如 `https://philosophy-globe-<hash>.vercel.app`（preview URL）。

- [ ] **Step 5: 部署到生产环境**

```bash
npx vercel --prod --yes
```

Expected: 输出生产 URL，形如 `https://philosophy-globe.vercel.app`。

- [ ] **Step 6: 打开生产 URL，人工验证**

同 Task 9 Step 3 的 8 条验证清单，全部在生产 URL 上再走一遍。

- [ ] **Step 7: 把生产 URL 写进 `README.md`**

把 README 里的 `👉 在线演示：待 Vercel 部署后填入` 替换为 `👉 在线演示：<生产 URL>`。

```bash
git add README.md
git commit -m "docs: add live demo URL to README"
git push
```

- [ ] **Step 8: 验收**

向 human partner 汇报：
- GitHub 仓库 URL
- Vercel 生产 URL
- 本地所有测试通过数
- 任何在 Task 7/9 中绕过的坑（如 `globeImageUrl` 方案切换）

---

## 验收清单（P1 完成标准）

- [ ] `npm run dev` 能启动开发服务器
- [ ] `npm run build` 成功且无 warning
- [ ] `npm run test:run` 所有测试通过（预期 8 个）
- [ ] `npm run typecheck` 无 error
- [ ] 本地浏览器可看到旋转深色地球 + 10 个 emoji + hover tooltip
- [ ] Vercel 生产 URL 可访问，行为与本地一致
- [ ] GitHub 仓库 public 可访问
- [ ] README 包含生产 URL

## P1 完成后的下一步

不在本计划范围，但为了决策连贯：

1. 用户点评 P1（视觉 / 交互 / 性能）
2. 写 P2 的 plan：数据模型完整化 + Tier 1 数据生成（~80 哲学家 / ~400 思想点）
3. 进入 P2 执行
